# Split
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# Sleep / deep sleep on idle (ms). Pick what you want:
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=600000   # 10 minutes

# Battery reporting (dt node vbatt is already present)
CONFIG_ZMK_BATTERY=y
